name: 'ecommerce_de_project'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_de_project'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_de_project:
    staging:
      +materialized: view        

    intermediate:
      +materialized: view        

    mart:
      +materialized: table       
